# Build outputs
build/
.gradle/
.kotlin/
out/
bin/
target/

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/

# OS
.DS_Store
Thumbs.db

# Local secrets (the placeholder index .env.example is tracked)
.env
!.env.example

# Agent worktrees. Each is a full checkout of this repository (12MB+ at three agents), with a .git file pointing at
# local absolute paths. Nothing here belongs in a public repository, and a broad `git add -A` would otherwise sweep
# entire duplicate trees into it.
.claude/

# Prompt-02 documentation bundle (duplicate of docs/)
/zerosum-ledger-docs.zip
.gstack/
